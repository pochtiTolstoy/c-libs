#ifndef STRTOK_DATA_H
#define STRTOK_DATA_H
#include "../../s21_string.h"
const char* strbuff[] = {
    "Hello, world!",  // Простая строка с одним разделителем
    "Hello,,world",  // Два подряд разделителя
    "123 456 789",   // Разделение чисел пробелами
    "a,b,c,d,e",  // Строка с несколькими разделителями
    "NoDelimitersHere",  // Без разделителей
    ",StartWithDelimiter",  // Строка начинается с разделителя
    "EndWithDelimiter,",  // Строка заканчивается разделителем
    "   LeadingSpaces",   // Строка с пробелами в начале
    "TrailingSpaces   ",  // Строка с пробелами в конце
    "   BothSides   ",    // Пробелы с обеих сторон
    "Repeating;;;Delimiters",  // Повторяющиеся разделители
    "Newline\ntest\nstring",  // Строка с символами новой строки
    "Tab\tseparated\tvalues",  // Строка с табуляцией
    "",                        // Пустая строка
    " ",                       // Строка с одним пробелом
    "A single character",      // Одна буква в строке
    "Special$Chars@Example#Test",  // Строка со специальными символами
    "Mix123andWords456",     // Числа и слова вместе
    "CaseInsensitiveCHECK",  // Разный регистр символов
    "Multiple spaces   between",  // Несколько пробелов подряд
    "Two,,Delimiters,Here",  // Два подряд идущих разделителя
    "Extra ,spaces ,with ,commas",  // Разделители и пробелы
    ",,,",                          // Только разделители
    ".,!?:;",  // Несколько различных разделителей
    "UTF8 пример строки",  // Строка на другом языке
    "a,b;c:d;e",  // Несколько типов разделителей
    "1;2;3;4;5",  // Числа с точкой с запятой
    "Nested (parentheses)",  // Строка со скобками
    "\"Quoted,Text\"",       // Строка с кавычками
    "Escape\\Characters\\Test",  // Строка с экранированными символами
    "Multiline\nstring\ntest",  // Многострочная строка
    "Delimiter|in|the|middle",  // Разделители в середине строки
    "|Leading|delimiter",  // Разделитель в начале строки
    "Trailing|delimiter|",  // Разделитель в конце строки
    "NoDelimiterButLengthyString",  // Длинная строка без разделителей
    "Short",                        // Очень короткая строка
    "Some!random.words-here",  // Случайные разделители
    "Mix of spaces and-tabs\t",  // Смешанные пробелы и табуляции
    "\t",                        // Только табуляция
    "\n",                        // Только новая строка
    "   ",                       // Только пробелы
    "Привет;мир;как;дела",  // Строка с разделителями на русском
    "123,456;789|000",  // Числа с разными разделителями
    "a@b#c$d%f^g&h*i",  // Разделители — специальные символы
    "OneToken",  // Одна целая строка
    " | ",       // Разделитель — пробел с символом
    "Split  sentence!",  // Простая строка с символами
    "Mixed:chars;tabs\twords",  // Смешанные символы и табуляции
    "I\0love\0nulls\0",
    "End-of-test",  // Завершающая строка для проверки
};

const char* delimsbuff[] = {
    " ,",       // Пробел и запятая
    ",",        // Только запятая
    " ",        // Только пробел
    ",;",       // Запятая и точка с запятой
    "",         // Нет разделителей
    ",",        // Только запятая
    ",",        // Только запятая
    " ",        // Только пробел
    " ",        // Только пробел
    " ",        // Только пробел
    ";",        // Только точка с запятой
    "\n",       // Новая строка
    "\t",       // Табуляция
    " ",        // Пробел
    " ",        // Пробел
    " ",        // Пробел
    "$@#",      // Специальные символы
    "123",      // Числовые разделители
    "CEH",      // Заглавные буквы
    " ",        // Пробел
    ",",        // Запятая
    " ,",       // Пробел и запятая
    ",",        // Запятая
    ".,!?:;",   // Разные символы
    " ",        // Пробел
    ";:",       // Точка с запятой и двоеточие
    ";",        // Точка с запятой
    "()",       // Скобки
    "\",",      // Кавычки и запятая
    "\\",       // Обратная косая черта
    "\n",       // Новая строка
    "|",        // Вертикальная черта
    "|",        // Вертикальная черта
    "|",        // Вертикальная черта
    "",         // Нет разделителей
    " ",        // Пробел
    "!-.",      // Специальные символы
    " \t",      // Пробел и табуляция
    "\t",       // Табуляция
    "\n",       // Новая строка
    " ",        // Пробел
    ";",        // Точка с запятой
    ",;|",      // Смешанные разделители
    "@#$%^&*",  // Специальные символы
    "",         // Нет разделителей
    "|",        // Вертикальная черта
    " ",        // Пробел
    ":;\t",  // Двоеточие, точка с запятой и табуляция
    "\0",
    "-"  // Тире
};

const s21_size_t size_strbuff = sizeof(strbuff) / sizeof(strbuff[0]);
const s21_size_t size_delimsbuff = sizeof(delimsbuff) / sizeof(delimsbuff[0]);

s21_size_t tests_num = 0;
#endif
